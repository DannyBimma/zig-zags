const std = @import("std");
const printer = std.debug.print; // Cool lil' tick

const TotalValues = struct {
    chars: u32,
    words: u32,
    sentences: u32,
    lines: u32,
};

pub fn main() !void {}
